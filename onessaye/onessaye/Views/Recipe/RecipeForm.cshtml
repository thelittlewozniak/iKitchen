@{
    string name = ViewBag.Name;
    int nbi = ViewBag.NbIngredients;
    var type = new List<SelectListItem> {
    new SelectListItem { Text = "Appetizer", Value = "Appetizer" },
    new SelectListItem { Text = "Main dish", Value = "Main dish" },
    new SelectListItem { Text = "Dessert", Value = "Dessert" }};
}
<h2>Create a new recipe</h2>
@if (ViewBag.ListErrors != null)
{
    <div>
        <ul>
            @foreach (string error in ViewBag.ListErrors)
            {
                <li>@error</li>
            }
        </ul>
    </div>
}
<div>
    @using (Html.BeginForm("CheckRecipe", "Recipe", "post"))
    {
    <fieldset>
        @Html.Label("name", "Name : ")
        @Html.TextBox("name", name , new { autofocus = "autofocus", placeholder = "Name your recipe", maxlength = 20, size = 20, required = "required"})
        <br />
        <h4>Select the dish type</h4>
        @Html.ListBox("type", type, new { required = "required"})
        <br />
        <h4>List of Ingredients</h4>
        <table>
            <tr>
                <th>Ingredient</th>
                <th>Amount</th>
                <th>Unit Price</th>
            </tr>
            @for (int i = 0; i < nbi; i++)
             {
                 <tr>
                    <td><input type="text" value="@ViewBag.ListIngredients[i]" name="ingredient[@i]" maxlength="15" size="15" required /></td>
                    <td><input type="number" name="amount[@i]" maxlength="2" size="2" min="1" max="20" required /></td>
                    <td><input type="text" value="@ViewBag.ListPrices[i]" name="price[@i]" maxlength="5" size="5" required /></td>
                 </tr>
             }
        </table>
        <p>@Html.ActionLink("+Add Ingredient Slot", "DisplayRecipeForm", new { NbIngredients = Convert.ToString(nbi + 1) }) 
        @Html.ActionLink("-Delete Ingredient Slot", "DisplayRecipeForm", new { NbIngredients = Convert.ToString(nbi - 1) })</p>
        <br />
        <table border="1">
            <tr>
                <th>Cost price</th>
                <td>@ViewBag.CostPrice</td>
            </tr>
            <tr>
                <th>Selling price</th>
                <td>@ViewBag.SellingPrice</td>
            </tr>
        </table>
        <br />
        <input type="submit" value="Create recipe" />
        @Html.RouteLink("Reset Form", "RecipeForm")
    </fieldset>
    }
</div>

