@model onessaye.Views.ViewClasses.DisplayCookInformations
<h2>Profile</h2>
<div>
    <table border="1">
        @for(int i=0;i<Model.TabLabels.Length; i++)
        {
            <tr>
                <th>@Model.TabLabels[i]</th>
                <td>@Model.TabInfos[i]</td>
            </tr>
        }
    </table>
</div>

